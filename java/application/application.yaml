spring:
  datasource:
    url: jdbc:mysql://localhost:3306/products_db
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  redis:
    host: localhost
    port: 6379

spring:
  graphql:
    schema:
      locations: classpath:graphql/