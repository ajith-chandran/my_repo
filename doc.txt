/**
 * Utility class for end-to-end test helpers.
 * <p>
 * Provides shared static methods that support dynamic test behaviors 
 * such as resolving the name of the currently executing test method at runtime.
 * This is commonly used to reflect method-specific test setup or logging.
 */


    /**
     * Retrieves the name of the currently executing method from the call stack.
     * <p>
     * This method inspects the current thread's stack trace and returns the method
     * name that invoked this utility (two levels up the call stack).
     * 
     * @return the name of the method calling this utility
     */
    