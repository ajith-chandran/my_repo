/**
 * Initializes the start page for Consumer UI tests.
 * <p>
 * This method performs the following actions:
 * <ul>
 *   <li>Loads the consumer homepage</li>
 *   <li>Adds the {@code page_consumerHome} tag if {@code @ReuseBrowser} annotation is present</li>
 *   <li>Resolves the currently executing test method using reflection</li>
 *   <li>Initializes the test domain and sets browser state as initialized</li>
 *   <li>Waits for CT object to be available in the browser window context</li>
 *   <li>Sets a pseudo session ID for client tracking</li>
 *   <li>Updates global session settings if not in PROD environment</li>
 * </ul>
 * 
 * @throws RuntimeException if the test method name cannot be resolved using reflection
 */